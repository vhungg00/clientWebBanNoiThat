import React from 'react';
import styled from 'styled-components';

function ListView({ loadProducts }) {
    return (
        <Wrapper>
            <div className="list-view">
                <div className="row">

                </div>
            </div>
        </Wrapper>
    );
}

const Wrapper = styled.div `
`

export default ListView;